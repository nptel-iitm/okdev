interface:
  display_name: "AgentForge Kickoff"
  short_description: "Master orchestrator for the full AgentForge workflow"
  default_prompt: "Use $kickoff to run the full AgentForge workflow for this project."

policy:
  allow_implicit_invocation: true
