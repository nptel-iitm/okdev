interface:
  display_name: "MegaDev Kickoff"
  short_description: "Master orchestrator for the full MegaDev workflow"
  default_prompt: "Use $kickoff to run the full MegaDev workflow for this project."

policy:
  allow_implicit_invocation: true
