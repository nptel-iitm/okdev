FROM python:3.12-slim
WORKDIR /srv
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PORT=8080 NOTES_DB=/data/notes.db
EXPOSE 8080
CMD ["python", "-m", "app.server"]
