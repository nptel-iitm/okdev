$kickoff

Continue the LoanBox build. A previous session was interrupted; the durable run
state in .okdev/run-state.json is the record of what is already finished.

Environment:
- GitLab is at http://gitlab.local:8929. The API token is in the GITLAB_TOKEN
  environment variable (send it as the PRIVATE-TOKEN header). The project
  okdev/loanbox holds the issues, merge requests and merged history.
  `git push origin` already has credentials in the remote URL.
- Docker is available and the daemon socket is mounted. Containers you start
  are siblings of this one, not children. This workspace is mounted at the same
  absolute path inside and outside this container, so relative bind mounts in a
  compose file resolve correctly.
- Ports a container publishes appear on the Docker host, not on this
  container's localhost. Reach your own stack at http://host.docker.internal:<port>.
- Playwright and its browsers are installed. PLAYWRIGHT_BROWSERS_PATH is set.
  Run headless.
- Sub-agents are available. Delegate as your instructions describe.

Do not redo a phase that run state lists as finished, and do not re-file issues
that already exist. Carry on from the current phase: finish the remaining
issues one at a time, merging each before starting the next, then run the
testing and delivery phases and complete the run.

Report what merged, what is open, and your readiness verdict.
