$bugfix

Fix issue #6 of okdev/labslots2:
http://gitlab.local:8929/okdev/labslots2/-/issues/6

This issue was filed by your own testing phase, with reproduction steps and
screenshot evidence in the description.

Environment:
- GitLab is at http://gitlab.local:8929. The API token is in the GITLAB_TOKEN
  environment variable (send it as the PRIVATE-TOKEN header). `git push origin`
  already has credentials in the remote URL.
- Docker is available and the daemon socket is mounted. Containers you start
  are siblings of this one, not children. This workspace is mounted at the same
  absolute path inside and outside this container, so relative bind mounts in a
  compose file resolve correctly.
- Ports a container publishes appear on the Docker host, not on this
  container's localhost. Reach the stack at http://host.docker.internal:<port>.
- Playwright and its browsers are installed. PLAYWRIGHT_BROWSERS_PATH is set.
  Run headless.
- Sub-agents are available. Delegate as your instructions describe.

Take it through to a merged MR with a regression test, and report what merged.
